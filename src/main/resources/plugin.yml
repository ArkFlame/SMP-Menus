name: ${project.name}
version: ${project.version}
main: ${plugin.main}
folia-supported: true
author: ${plugin.author}
description: ${project.description}
api-version: 1.13
softdepend:
  - PlaceholderAPI
permissions:
  smpmenus.use:
    default: true
  smpmenus.admin:
    default: op
  smpmenus.staff:
    default: op
  smphelp.use:
    default: true
  smphelp.admin:
    default: op
